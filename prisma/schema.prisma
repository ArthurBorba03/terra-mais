// ═══════════════════════════════════════════════════════════
// INSTRUÇÕES: Adicione esses trechos ao prisma/schema.prisma
// ═══════════════════════════════════════════════════════════

// 1. Adicione os novos enums (após os enums existentes):

enum SaleType {
  ONLINE
  PRESENTIAL
}

enum SalePayment {
  CASH
  PIX
  CREDIT_CARD
  DEBIT_CARD
}

// 2. Adicione o model Sale (no final do arquivo):

model Sale {
  id            String      @id @default(cuid())
  type          SaleType    @default(PRESENTIAL)
  orderId       String?     @unique
  productId     String?
  productName   String
  productImg    String?
  quantity      Int         @default(1)
  unitPrice     Decimal     @db.Decimal(10, 2)
  total         Decimal     @db.Decimal(10, 2)
  customerName  String?
  customerPhone String?
  paymentMethod SalePayment @default(CASH)
  notes         String?
  soldAt        DateTime    @default(now())
  createdAt     DateTime    @default(now())

  order   Order?   @relation(fields: [orderId], references: [id], onDelete: SetNull)
  product Product? @relation(fields: [productId], references: [id], onDelete: SetNull)

  @@index([soldAt(sort: Desc)])
  @@index([type])
  @@map("sales")
}

// 3. No model Order, adicione a relação:
//    sale    Sale?

// 4. No model Product, adicione a relação:
//    sales   Sale[]


model Sale {
  id            String      @id @default(cuid())
  type          SaleType    @default(PRESENTIAL)
  orderId       String?     @unique
  productId     String?
  productName   String
  productImg    String?
  quantity      Int         @default(1)
  unitPrice     Decimal     @db.Decimal(10, 2)
  total         Decimal     @db.Decimal(10, 2)
  customerName  String?
  customerPhone String?
  paymentMethod SalePayment @default(CASH)
  notes         String?
  soldAt        DateTime    @default(now())
  createdAt     DateTime    @default(now())

  order   Order?   @relation(fields: [orderId], references: [id], onDelete: SetNull)
  product Product? @relation(fields: [productId], references: [id], onDelete: SetNull)

  @@index([soldAt(sort: Desc)])
  @@index([type])
  @@map("sales")
}

enum SaleType {
  ONLINE
  PRESENTIAL
}

enum SalePayment {
  CASH
  PIX
  CREDIT_CARD
  DEBIT_CARD
}